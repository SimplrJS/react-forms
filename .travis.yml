language: node_js
notifications:
  email:
    on_success: never
    on_failure: change
node_js:
- stable
before_install:
- "npm install -g npm@^4.6.1"
- "npm install -g typescript@^2.3"
script:
- npm run generate
- npm run tools-build
- npm run source-build
- npm test
