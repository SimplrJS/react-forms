{
  "name": "@simplr/react-forms-repo",
  "private": true,
  "scripts": {
    "rush-install": "rush install",
    "tools-build": "npm run rush-tools -- run -s gulp-build -e @simplr/mvdir -e @simplr/extractor",
    "source-build": "npm run rush-tools -- run -s build -e @simplr/mvdir -e @simplr/extractor",
    "test": "npm run rush-tools -- run -s test -e @simplr/mvdir -e @simplr/extractor",
    "publish": "npm run rush-tools -- publish --access public",
    "rush-tools": "ts-node ./tools/rush-tools.ts"
  },
  "devDependencies": {
    "@microsoft/rush": "^3.0.11",
    "@microsoft/rush-lib": "^3.0.11",
    "@types/shelljs": "^0.7.2",
    "mz": "^2.6.0",
    "shelljs": "^0.7.8",
    "ts-node": "^3.1.0",
    "typescript": "^2.4.1",
    "@microsoft/ts-command-line": "^2.0.2",
    "@types/semver": "^5.3.32",
    "semver": "^5.3.0"
  },
  "dependencies": {}
}
