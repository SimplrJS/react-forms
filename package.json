{
  "name": "@simplr/react-forms-repo",
  "private": true,
  "scripts": {
    "rush-install": "rush install",
    "tools-build": "npm run rush-tools -- run gulp-build -e @simplr/mvdir -e @simplr/extractor",
    "source-build": "npm run rush-tools -- run build -e @simplr/mvdir -e @simplr/extractor",
    "test": "npm run rush-tools -- run test -e @simplr/mvdir -e @simplr/extractor",
    "rush-tools": "ts-node ./tools/rush-tools.ts",
    "publish": "npm run rush-tools -- publish --access public"
  },
  "devDependencies": {
    "@microsoft/rush": "^3.0.11",
    "@microsoft/rush-lib": "^3.0.11",
    "@types/shelljs": "^0.7.2",
    "@types/yargs": "^8.0.0",
    "mz": "^2.6.0",
    "shelljs": "^0.7.8",
    "ts-node": "^3.1.0",
    "typescript": "^2.4.1",
    "yargs": "^8.0.2"
  }
}
