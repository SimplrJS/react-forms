{
    "name": "forms",
    "private": true,
    "scripts": {
        "build": "lerna run build",
        "test": "lerna run test",
        "bootstrap": "lerna bootstrap",
        "pack": "lerna exec -- npm pack"
    },
    "devDependencies": {
        "lerna": "3.16.4",
        "npm-run-all": "4.1.5"
    }
}
