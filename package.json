{
  "name": "simplr-forms",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test-tsc && lerna run test-tslint"
  },
  "devDependencies": {
    "lerna": "2.0.0-rc.2"
  }
}
